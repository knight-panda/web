export interface AdminStoreDomainRequest {
    storeName: string;
    storeDescription: string;
    domain: string;
    subdomain: string;
    logoUrl: string;
    faviconUrl: string;
    primaryColor: string;
    secondaryColor: string;
}